package com.school.canteen.enums;

/** Dietary classification, kept separate from {@link MenuCategory} so the veg filter and
 *  the category filter are independent. */
public enum FoodType {
    VEG,
    NON_VEG
}
