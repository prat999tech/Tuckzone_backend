package com.school.canteen.enums;

/** Overhead buckets, so the canteen can see where money goes beyond ingredients. */
public enum ExpenseCategory {
    INGREDIENTS,
    STAFF_WAGES,
    GAS_FUEL,
    RENT,
    UTILITIES,
    PACKAGING,
    EQUIPMENT,
    OTHER
}
