package com.school.canteen.enums;

/** How the customer receives their food. */
public enum OrderType {
    /** Delivered to a classroom or staff room. */
    DELIVERY,
    /** Collected at the counter by showing a pickup code. */
    TAKEAWAY
}
